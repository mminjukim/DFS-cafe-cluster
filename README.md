# ☕ DFS를 이용한 성북구 카페 거리 분석

## 1\. 📌 문제 정의

일반적인 상권 분석은 '동' 단위의 행정 구역을 기준으로 이루어집니다. 하지만 실제 소비자가 체감하는 '카페 거리'나 '핫플레이스'는 행정 구역 경계와 무관하게 **물리적으로 인접한 가게들의 집합**으로
형성됩니다.

본 프로젝트는 다음과 같은 목표로 시작되었습니다.

* **지리적 군집화**: 위/경도 좌표를 기반으로 인접한 카페들을 하나의 '군집(Cluster)'으로 묶습니다.
* **동적 분석**: 고정된 구역이 아닌, 사용자가 설정한 '반경(거리 임계값)'에 따라 실시간으로 변화하는 상권을 시각화합니다.
* **유의미한 데이터 필터링**: 일정 규모(5곳 이상) 이상이 뭉쳐 있는 실질적인 '카페 거리'를 도출합니다.

-----

## 2\. 📊 사용 데이터

* **출처**: [공공데이터포털] 소상공인시장진흥공단\_상가(상권)정보
* **범위**: 서울특별시 성북구 내 업종 분류가 '카페'인 데이터
* **주요 속성**:
    * 상호명: 카페 이름
    * 경도, 위도: 좌표 기반 거리 계산 데이터
    * 도로명주소: 사용자 정보 제공용

-----

## 3\. 🛠 구현 방법 및 알고리즘

### 3.1. 핵심 알고리즘

카페 데이터를 그래프로 모델링하고, 연결 요소(Connected Components)를 찾아내는 프로그램을 작성했습니다.

1. **그래프 구축**:
    * **Node**: 개별 카페
    * **Edge**: 두 카페 사이의 거리가 임계값(Threshold, 예: 50m) 이내일 경우 연결
2. **군집 탐색 (DFS)**:
    * 방문하지 않은 노드에서 DFS 탐색을 시작합니다.
    * 연결된 모든 노드(카페)를 재귀적으로 방문하며 하나의 리스트(`List<CoffeeShop>`)에 담습니다.
    * 탐색이 종료되면 하나의 군집이 완성됩니다.
3. **필터링**:
    * 탐색이 끝난 군집의 크기가 5 미만인 경우, 유의미한 상권이 아니라고 판단하여 결과에서 제외합니다.

### 3.2. 거리 계산 로직

하버사인(Haversine) 공식을 적용하여 미터 단위의 거리를 계산했습니다.

### 3.3. Tech Stack

* **Backend**: Java 21, Spring Boot 4.0.0, Spring Data JPA
* **Frontend**: HTML5, CSS3, Thymeleaf
* **Database**: H2
* **Etc**: OpenCSV, Lombok

-----

## 4\. 💻 실행 결과

![img.png](img.png)

-----

## 5\. 🏃‍♂️ 실행 방법

이 프로젝트는 로컬 환경에서 실행해 볼 수 있도록 구성되어 있습니다.

### 5.1. 사전 요구 사항

* Java 21 이상이 설치되어 있어야 합니다.
* Git이 설치되어 있어야 합니다.

### 5.2. 설치 및 실행

**1. 리포지토리 클론**

```bash
git clone https://github.com/mminjukim/DFS-cafe-cluster.git
cd DFS-cafe-cluster
```

**2. 프로젝트 실행**

프로젝트 루트 디렉토리에서 다음 명령어를 실행합니다.

* **Mac / Linux**

  ```bash
  ./gradlew bootRun
  ```

* **Windows**

  ```cmd
  gradlew.bat bootRun
  ```

**3. 브라우저 접속**

서버가 정상적으로 실행되면 브라우저를 열고 `http://localhost:8080` 주소로 접속합니다.



-----

### 📂 Directory Structure

```
src
├── main
│   ├── java/com/algorithm/project
│   │   ├── domain         # CoffeeShop Entity
│   │   ├── repository     # JPA Repository
│   │   ├── infrastructure # CSV data initializer
│   │   ├── service        # ForestService (DFS Logic)
│   │   └── controller     # Web Controller
│   └── resources
│       ├── static/css     # style.css
│       └── templates      # forest-view.html (Thymeleaf View)
```